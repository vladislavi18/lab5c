#ifndef LAB5C_ALGORITHMS_H
#define LAB5C_ALGORITHMS_H

#include <stdio.h>
#include <stdbool.h>
void inputArray(int *a, size_t size);

void outputArray(int *a, size_t size);

void swap(int *a, int *b);

bool isOrdered(const int *a, size_t size);

#endif
